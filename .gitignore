# Dependencies
node_modules/

# Environment files with real secrets — never commit these.
# Only .env.example files (no real values) should be tracked.
.env
.env.local
backend/.env
mobile/.env

# Expo
mobile/.expo/
mobile/dist/
mobile/web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Native build output (regenerated by prebuild/EAS, not hand-maintained)
mobile/ios/
mobile/android/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
